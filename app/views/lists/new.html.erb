<div class="container">
  <h1>Create a new list</h1>

  <%= simple_form_for @list do |f| %>
    <%= f.input :name %>
    <%= f.submit "Create a list", class: 'btn btn-success' %>
    <%= link_to 'Back', root_path, class: 'btn btn-secondary' %>
  <% end %>
</div>
